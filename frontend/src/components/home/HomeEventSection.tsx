/**
 * 홈 진행 중 이벤트 영역. `newsListQueryOptions('EVENT', 'LATEST', { eventStatus: 'ONGOING', size: 2 })`로
 * 직접 조회하고, 항목은 `NewsItem`으로 그린다.
 *
 * 헤더의 "소식 더보기"는 `/news`로 연결한다.
 */
export function HomeEventSection() {
  // TODO 테스트 검토 후 구현
  return null;
}
