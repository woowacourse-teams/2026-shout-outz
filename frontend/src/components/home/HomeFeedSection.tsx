/**
 * 홈 피드 영역. `feedListQueryOptions({ sort, size: 3 })`로 직접 조회한다.
 *
 * 최신순/인기순 탭 상태는 URL에 남기지 않고 컴포넌트 안에서 관리한다. 기본은 최신순.
 * 헤더의 "피드 전체보기"는 `/feeds`로 연결한다.
 */
export function HomeFeedSection() {
  // TODO 테스트 검토 후 구현
  return null;
}
