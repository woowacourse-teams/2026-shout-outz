import { type FeedAuthor } from '@/types/feed';

/**
 * 피드 카드 작성자 줄에 쓰는 표시 문구.
 *
 * - 기수와 트랙이 모두 있으면: "황호익 · 6기 백엔드"
 * - 둘 중 하나라도 없으면: "황호익"
 */

export function formatAuthorLabel(author: FeedAuthor) {}
